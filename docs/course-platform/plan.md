# Implementation slices

1. Install Scaffold harness and establish language, build, and preview conventions.
2. Migrate browser modules to TypeScript while preserving progress schema and page URLs.
3. Add broad-audience onboarding and definition-first concept visuals and external references.
4. Validate learning interactions, formatting, audio, mobile layout, and static hosting; publish the result.

## Learner-specific hardware and persistence follow-up

Replace the contributor-specific hardware guide with browser-local hardware and budget choices, retaining the original setup only as a labeled case study. Preserve learning data per browser profile; bookmarks carry location, while full export/import carries attempts, confidence, module notes, lab/submodule notes and hardware choices. Validate backup records before changing storage, retain old-backup compatibility, and test isolation across browser contexts.
