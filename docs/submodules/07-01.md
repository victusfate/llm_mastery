# Understand the mechanism

Module 7: Policy gradients · Submodule 07-01

[Module overview](../../site/?module=7) · [Complete module reference](../../modules/07-rl.md)

For a policy pi_theta, maximize expected return `J(theta) = E_tau[R(tau)]`. The score-function identity yields an estimator using `grad log pi_theta(a|s)` multiplied by a return or advantage estimate. An action-independent baseline can reduce variance without changing the expected policy gradient under the appropriate sampling assumptions. Read the [policy-optimization derivation](https://spinningup.openai.com/en/latest/spinningup/rl_intro3.html).

Define MDP, reward, discounted return, value, Q, advantage, credit assignment, exploration, entropy, on/off-policy data, bootstrapping, and importance sampling. For GAE, connect TD residuals and the gamma/lambda bias–variance choice. Distinguish true termination from a time-limit truncation.

PPO uses a ratio to the rollout policy and a clipped surrogate. Read the [PPO paper](https://arxiv.org/abs/1707.06347); clipping is an optimization device, not a hard guarantee of a small KL divergence.

## Put it into practice

Listen to the short clip alongside the text. Use the interactive example to change one variable and explain the result. Then open the relevant lab for implementation guidance. The visualization is a teaching model, not a measured training result.

- [Lab 07-01](../../site/lab.html?lab=07-01)
- [Lab 07-02](../../site/lab.html?lab=07-02)
- [Lab 07-03](../../site/lab.html?lab=07-03)
- [Lab 07-04](../../site/lab.html?lab=07-04)

## Retrieval and transfer

Close the explanation and state the mechanism in your own words. Name one assumption, one likely implementation failure, and one observation that would reveal it. Then modify a small example and predict the outcome before running. Use the dedicated lab’s completion criteria for practical assessment.

## Spoken transcript

Welcome to Policy gradients. This first section explains the mechanism before you implement it. For a policy pi_theta, maximize expected return J(theta) = E_tau[R(tau)]. The score-function identity yields an estimator using grad log pi_theta(a|s) multiplied by a return or advantage estimate. An action-independent baseline can reduce variance without changing the expected policy gradient under the appropriate sampling assumptions. Read the policy-optimization derivation.

Define MDP, reward, discounted return, value, Q, advantage, credit assignment, exploration, entropy, on/off-policy data, bootstrapping, and importance sampling. For GAE, connect TD residuals and the gamma/lambda bias–variance choice. Distinguish true termination from a time-limit truncation.

PPO uses a ratio to the rollout policy and a clipped surrogate. Read the PPO paper; clipping is an optimization device, not a hard guarantee of a small KL divergence. Pause after the explanation. Open any unfamiliar term, inspect the interactive example, and describe the mechanism in your own words before moving to the experiment.
