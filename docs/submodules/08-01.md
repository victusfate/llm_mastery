# Understand the mechanism

Module 8: Preferences and RLHF · Submodule 08-01

[Module overview](../../site/?module=8) · [Complete module reference](../../modules/08-alignment.md)

A common pairwise reward objective is `-log sigmoid(r(x,y_chosen)-r(x,y_rejected))`. Preference labels can encode annotator biases, ambiguity, and noise. Synthetic preferences are useful for mechanics but are not human feedback.

For a fixed reference policy, define `delta = [log pi_theta(y_c|x)-log pi_ref(y_c|x)] - [log pi_theta(y_r|x)-log pi_ref(y_r|x)]`. DPO minimizes `-log sigmoid(beta * delta)`. Sequence log probabilities sum over response tokens; substituting a mean changes the objective and must be labeled. Read [DPO](https://arxiv.org/abs/2305.18290).

In PPO-based RLHF, distinguish the **current trainable policy**, the **old rollout policy**, the **fixed reference**, the **reward model**, and the **value estimator**. The old policy supplies importance ratios; the reference constrains drift. Read [InstructGPT](https://arxiv.org/abs/2203.02155) for the overall workflow.

## Put it into practice

Listen to the short clip alongside the text. Use the interactive example to change one variable and explain the result. Then open the relevant lab for implementation guidance. The visualization is a teaching model, not a measured training result.

- [Lab 08-01](../../site/lab.html?lab=08-01)
- [Lab 08-02](../../site/lab.html?lab=08-02)
- [Lab 08-03](../../site/lab.html?lab=08-03)
- [Lab 08-04](../../site/lab.html?lab=08-04)
- [Lab 08-05](../../site/lab.html?lab=08-05)
- [Lab 08-06](../../site/lab.html?lab=08-06)

## Retrieval and transfer

Close the explanation and state the mechanism in your own words. Name one assumption, one likely implementation failure, and one observation that would reveal it. Then modify a small example and predict the outcome before running. Use the dedicated lab’s completion criteria for practical assessment.

## Spoken transcript

Welcome to Preferences and RLHF. This first section explains the mechanism before you implement it. A common pairwise reward objective is -log sigmoid(r(x,y_chosen)-r(x,y_rejected)). Preference labels can encode annotator biases, ambiguity, and noise. Synthetic preferences are useful for mechanics but are not human feedback.

For a fixed reference policy, define delta = [log pi_theta(y_c|x)-log pi_ref(y_c|x)] - [log pi_theta(y_r|x)-log pi_ref(y_r|x)]. DPO minimizes -log sigmoid(beta * delta). Sequence log probabilities sum over response tokens; substituting a mean changes the objective and must be labeled. Read DPO.

In PPO-based RLHF, distinguish the **current trainable policy**, the **old rollout policy**, the **fixed reference**, the **reward model**, and the **value estimator**. The old policy supplies importance ratios; the reference constrains drift. Read InstructGPT for the overall workflow. Pause after the explanation. Open any unfamiliar term, inspect the interactive example, and describe the mechanism in your own words before moving to the experiment.
