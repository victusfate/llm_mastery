# Understand the mechanism

Module 2: Build a transformer · Submodule 02-01

[Module overview](../../site/?module=2) · [Complete module reference](../../modules/02-transformer.md)

Causal modeling factors a sequence probability as `product_t p(x_t | x_<t)`. Attention uses `softmax(Q K^T / sqrt(d_head) + causal_mask) V`. With B batch, H heads, T sequence length, and d head width, Q/K/V are B×H×T×d, and scores are B×H×T×T.

Learn byte-level tokenization and BPE, embeddings, residuals, LayerNorm/RMSNorm, positional information including RoPE, gated feed-forward layers, multi-head versus grouped-query attention, tied embeddings, sampling temperature, and KV caching. Build a minimal correct architecture first; add one modern component at a time. The original [Transformer paper](https://arxiv.org/abs/1706.03762) is the attention reference; [CS336](https://cs336.stanford.edu/) provides modern language-model context.

## Put it into practice

Listen to the short clip alongside the text. Use the interactive example to change one variable and explain the result. Then open the relevant lab for implementation guidance. The visualization is a teaching model, not a measured training result.

- [Lab 02-01](../../site/lab.html?lab=02-01)
- [Lab 02-02](../../site/lab.html?lab=02-02)
- [Lab 02-03](../../site/lab.html?lab=02-03)
- [Lab 02-04](../../site/lab.html?lab=02-04)

## Retrieval and transfer

Close the explanation and state the mechanism in your own words. Name one assumption, one likely implementation failure, and one observation that would reveal it. Then modify a small example and predict the outcome before running. Use the dedicated lab’s completion criteria for practical assessment.

## Spoken transcript

Welcome to Build a transformer. This first section explains the mechanism before you implement it. Causal modeling factors a sequence probability as product_t p(x_t | x_<t). Attention uses softmax(Q K^T / sqrt(d_head) + causal_mask) V. With B batch, H heads, T sequence length, and d head width, Q/K/V are B×H×T×d, and scores are B×H×T×T.

Learn byte-level tokenization and BPE, embeddings, residuals, LayerNorm/RMSNorm, positional information including RoPE, gated feed-forward layers, multi-head versus grouped-query attention, tied embeddings, sampling temperature, and KV caching. Build a minimal correct architecture first; add one modern component at a time. The original Transformer paper is the attention reference; CS336 provides modern language-model context. Pause after the explanation. Open any unfamiliar term, inspect the interactive example, and describe the mechanism in your own words before moving to the experiment.
