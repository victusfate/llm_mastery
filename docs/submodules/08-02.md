# Walk through the experiment

Module 8: Preferences and RLHF · Submodule 08-02

[Module overview](../../site/?module=8) · [Complete module reference](../../modules/08-alignment.md)

1. Build a small preference dataset with known construction rules and a held-out split by prompt family. Include deliberately noisy labels and length-confounded pairs. Train a reward head and audit accuracy, calibration, and reward-versus-length behavior.
2. Implement DPO directly from token log probabilities. Compare a tiny numerical case to a trusted implementation; test masking, identical policies, chosen/rejected reversal, and frozen reference gradients. Sweep beta and label noise.
3. Implement minimal text PPO on a tiny policy and short responses. Log reward, reference KL estimate, entropy, ratio/clip fraction, value loss, response length, and validity. Validate one update independently before larger runs. Then inspect a maintained trainer implementation.
4. Train SFT, DPO, and PPO variants from the same SFT reference where applicable. Report compute/data differences explicitly; preference training and online RL do not consume identical resources. Evaluate on the same held-out prompts and generation budget.
5. Create a proxy-reward failure, such as rewarding a format marker regardless of answer correctness. Show the divergence between proxy reward and true task success. Add a targeted mitigation and retest.
6. Use a benign behavior suite to inspect instruction following, spurious refusal, robustness to misleading instructions, and reward overoptimization. Discuss why these tests do not establish comprehensive alignment or safety.

## Put it into practice

Listen to the short clip alongside the text. Use the interactive example to change one variable and explain the result. Then open the relevant lab for implementation guidance. The visualization is a teaching model, not a measured training result.

- [Lab 08-01](../../site/lab.html?lab=08-01)
- [Lab 08-02](../../site/lab.html?lab=08-02)
- [Lab 08-03](../../site/lab.html?lab=08-03)
- [Lab 08-04](../../site/lab.html?lab=08-04)
- [Lab 08-05](../../site/lab.html?lab=08-05)
- [Lab 08-06](../../site/lab.html?lab=08-06)

## Retrieval and transfer

Close the explanation and state the mechanism in your own words. Name one assumption, one likely implementation failure, and one observation that would reveal it. Then modify a small example and predict the outcome before running. Use the dedicated lab’s completion criteria for practical assessment.

## Spoken transcript

In this section of Preferences and RLHF, turn the theory into a bounded experiment. Start with a tiny case and write its expected behavior before running code. Read the complete sequence below, then open the dedicated lab page for the next task. That page contains setup, step-by-step guidance, troubleshooting, and an evidence record. Keep the original baseline and introduce one change at a time. Record exact commands and configurations. If an observation disagrees with your prediction, propose two explanations and design the cheapest test that separates them. The interactive example illustrates a related mechanism; it is not a substitute for running and defending the lab. Use agents for tooling if helpful, but inspect the numerical and data assumptions yourself.
