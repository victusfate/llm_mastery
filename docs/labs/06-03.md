# Lab 06-03: Full versus adapter fine-tuning

[Return to the module](../../modules/06-sft.md) · [Open the dedicated lab page](../../site/lab.html?lab=06-03)

## Purpose and prerequisite

Work through the corresponding explanation in Module 6 first. Bring the preceding lab’s artifact if this exercise extends it. The objective of this lab is concrete: Train a full-parameter tiny baseline and an adapter variant under explicit budgets. Count trainable parameters, optimizer memory, tokens, and step time. On a larger open checkpoint, use the memory-saving option supported by the measured fit.

This is a guided assignment, not a completed result. Record your predictions and measured observations separately. The time spent here belongs inside the module’s allocation.

## Setup

Use a tiny policy for checks, then a measured-fit checkpoint on the local 5090. Start with the smallest input that exposes the mechanism. Create `work/labs/06-03/` for code, configs, and compact evidence; keep bulky data and checkpoints in the ignored top-level artifact directories. Save exact dependency and source revisions in your report.

## Guided procedure

1. Write a one-paragraph contract for **full versus adapter fine-tuning**. Specify inputs, outputs, units, assumptions, and the invariant you will check.
2. Make a tiny reference case whose expected result you can calculate or inspect independently. State that result before running code.
3. Carry out the assignment below in small steps. Keep the reference case passing after each change.

Train a full-parameter tiny baseline and an adapter variant under explicit budgets. Count trainable parameters, optimizer memory, tokens, and step time. On a larger open checkpoint, use the memory-saving option supported by the measured fit.

4. Record the baseline command/config and observed output. Introduce only the specified comparison or failure; preserve other variables and log intentional differences.
5. Investigate any disagreement using the diagnosis below. Retain the failing example as a regression check rather than deleting it once the code appears to work.
6. Write a short result: what you expected, what occurred, why you think it occurred, and what remains unproven.

## Expected observations

The output must expose the mechanism named in the assignment, including its edge cases. Do not target a universal benchmark number. Correctness checks should match the hand-computable/reference case within a declared tolerance; stochastic/performance comparisons must include their variation and workload definition. A negative improvement is acceptable if the method and diagnosis are sound.

## If the result is surprising

Print token/label pairs and active-token counts before trusting a trainer loss. Reduce to one example, one worker, or one update where appropriate. List two competing explanations and run the cheapest test that distinguishes them. Do not change several hyperparameters together.

## Evidence to keep

Save the minimal reproducible code, command, configuration/revisions, reference check, observed metrics or trace, failure diagnosis, and a short explanation in your own words. Link large artifacts rather than committing them. Label generated, paired, hinted, and independent portions.

## Completion and transfer check

The exercise is complete when its specific assignment requirements are demonstrated, the reference/edge checks pass, and you can explain the evidence. Change a template, truncation boundary, or response length and verify intended supervision. Ask your tutor to assess the result against the [mastery rubric](../../assessments/01-mastery.md); a saved lab note is not an automatic pass.

## Tutor handoff

> Review Lab 06-03, Full versus adapter fine-tuning. Read my prediction, exact command, and evidence. Ask me to explain the key mechanism, identify a hidden failure mode, and complete a fresh small transfer task. Grade only demonstrated understanding and distinguish agent-assisted construction from independent explanation.
