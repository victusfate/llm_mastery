# Lab 03-06: Checkpoint recovery and training observability

[Return to the module](../../modules/03-pretraining.md) · [Open the dedicated lab page](../../site/lab.html?lab=03-06)

## Purpose and prerequisite

Work through the corresponding explanation in Module 3 first. Bring the preceding lab’s artifact if this exercise extends it. The objective of this lab is concrete: Interrupt and resume training, including data position and random states. Log loss, gradient norm, learning rate, valid tokens, throughput, memory, and checkpoint metadata. Write an incident report for one deliberately injected failure.

This is a guided assignment, not a completed result. Record your predictions and measured observations separately. The time spent here belongs inside the module’s allocation.

## Setup

A small local document sample first; the 5090 for controlled training pilots. Start with the smallest input that exposes the mechanism. Create `work/labs/03-06/` for code, configs, and compact evidence; keep bulky data and checkpoints in the ignored top-level artifact directories. Save exact dependency and source revisions in your report.

## Guided procedure

1. Write a one-paragraph contract for **checkpoint recovery and training observability**. Specify inputs, outputs, units, assumptions, and the invariant you will check.
2. Make a tiny reference case whose expected result you can calculate or inspect independently. State that result before running code.
3. Carry out the assignment below in small steps. Keep the reference case passing after each change.

Interrupt and resume training, including data position and random states. Log loss, gradient norm, learning rate, valid tokens, throughput, memory, and checkpoint metadata. Write an incident report for one deliberately injected failure.

4. Record the baseline command/config and observed output. Introduce only the specified comparison or failure; preserve other variables and log intentional differences.
5. Investigate any disagreement using the diagnosis below. Retain the failing example as a regression check rather than deleting it once the code appears to work.
6. Write a short result: what you expected, what occurred, why you think it occurred, and what remains unproven.

## Expected observations

The output must expose the mechanism named in the assignment, including its edge cases. Do not target a universal benchmark number. Correctness checks should match the hand-computable/reference case within a declared tolerance; stochastic/performance comparisons must include their variation and workload definition. A negative improvement is acceptable if the method and diagnosis are sound.

## If the result is surprising

Reconcile input/kept/rejected counts and check split/group identities before interpreting loss. Reduce to one example, one worker, or one update where appropriate. List two competing explanations and run the cheapest test that distinguishes them. Do not change several hyperparameters together.

## Evidence to keep

Save the minimal reproducible code, command, configuration/revisions, reference check, observed metrics or trace, failure diagnosis, and a short explanation in your own words. Link large artifacts rather than committing them. Label generated, paired, hinted, and independent portions.

## Completion and transfer check

The exercise is complete when its specific assignment requirements are demonstrated, the reference/edge checks pass, and you can explain the evidence. Introduce a new source or planted corrupt record and defend how the pipeline handles it. Ask your tutor to assess the result against the [mastery rubric](../../assessments/01-mastery.md); a saved lab note is not an automatic pass.

## Tutor handoff

> Review Lab 03-06, Checkpoint recovery and training observability. Read my prediction, exact command, and evidence. Ask me to explain the key mechanism, identify a hidden failure mode, and complete a fresh small transfer task. Grade only demonstrated understanding and distinguish agent-assisted construction from independent explanation.
